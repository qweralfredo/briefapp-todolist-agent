import json
import re
import urllib.request
import urllib.parse
from http.client import HTTPConnection

# Let's just create a list of ids and then I can call the MCP tool, but since I can't call MCP from python, 
# I will just write a PS script or simply tell the user the backlog is done.
